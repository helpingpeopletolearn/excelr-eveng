pipeline {
	agent any

	stages {
		stage('Fetch Code') {
			steps {
				echo 'Fetching code'
			}
		}

		stage('Install apache') {
			steps {
				echo 'Install apache'
			}
		}

		stage('Deploy Application') {
			steps {
				echo 'Deploying Application'
			}
		}

	}
}
